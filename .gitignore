.DS_Store
**/.DS_Store
.env
.data.*
/hoonc
test-leader
test-follower
.nockchain_identity
.nockchain_identity.peerid
target
assets/*.jam
!assets/honc-cold-138.jam
!crates/nockapp/test-jams
keys.export
replay-checkpoints-51094
nockchain-api-checkpoints-backup
**/.DS_Store
notes-*.csv
/txs
/hoonc-octs-type-138.jam
assets/native/*.jam
bazel-*
*.jam

# Reference papers (cite by name only in docs; the PDFs themselves are
# not in the repo per maintainer directive 2026-05-19). Auditors are
# expected to obtain the papers via their citations (ePrint IDs, arXiv,
# venue, etc.).
*.pdf

# Python build/cache artifacts (none of these are source; they're
# generated by the Python interpreter or build tools).
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Python virtual environments — pearl_vllm_cpu/.venv etc.
# Use uv/pip/poetry to recreate from the project's requirements.
.venv/
venv/
env/
ENV/

# Profiling output
f1_profile_*.json.gz
*.prof

# Local model weights / checkpoints (large binaries; obtained out of
# band per the Pearl + ai-pow-zk setup docs).
*.safetensors
*.gguf
*.pt

# Worktrees managed by Claude Code (transient per-session work).
.claude/worktrees/
*.swp
pearl/
pearl

# proptest regression seeds (auto-generated during test runs)
**/proptest-regressions/
.fakenet-ai-pow-data/
